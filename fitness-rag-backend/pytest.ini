[pytest]
; test_garmin_connection.py is a manual, interactive connectivity script (not
; a pytest suite) that happens to match the test_*.py collection pattern and
; raises SystemExit at import time when garminconnect/credentials aren't
; available. Exclude it from automatic collection.
addopts = --ignore=test_garmin_connection.py
